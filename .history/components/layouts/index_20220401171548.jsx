// layout in react for the index page of the website

import 
const Layout = ({ children }) => {
    return (
    
        <Header />
  
        <main>{children}</main>
  
        <Footer />
   
    );
  };
  
  export default Layout;